#include <iostream>
#include <vector>
#include <utility>
#include <algorithm>
#include <cmath>

using namespace std;
vector<string> doing;
int main(){
	int T;

	cin>>T;
	for(int t=0;t<T;t++){
		int m;
		cin>>m;
		doing.clear();
		for(int i=0;i<m;i++){
			string tmp;
			cin>>tmp;
			doing.push_back(tmp);
		}
		run();
		for()
	}
}